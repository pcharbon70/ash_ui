This directory contains portable dashboard definitions for Ash UI telemetry.

Each JSON file describes a dashboard in terms of Ash UI event names, measurements,
and recommended aggregations so teams can map them into Grafana, LiveDashboard,
or another metrics backend.
